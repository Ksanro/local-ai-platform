[project]
name = "local-ai-platform-core"
version = "0.1.0"
description = "Core business logic for Local AI Platform"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"