[project]
name = "local-ai-platform-telemetry"
version = "0.1.0"
description = "Telemetry and monitoring for Local AI Platform"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"